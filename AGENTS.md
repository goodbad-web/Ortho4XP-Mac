# AGENTS.md

このリポジトリで作業するエージェント向けの共通ルールです。

## 優先順位
- 正確性、安全性、可読性、保守性を最優先
- 既存設計・既存データフローとの整合性を維持
- 小さな差分を優先。ただし品質を損なうなら必要最小限で広げる
- 推測で実装しない。未確認事項は仮説として扱う

## 作業前
- `TASK.md`、`BUG.md`、`SKILLS.md` を確認
- 関連ファイル、呼び出し元、依存先、状態管理、UI更新経路を必要に応じて確認
- 変更対象と影響範囲を短く整理してから着手

## 実装方針
- 関係ないファイルは触らない
- 不要な rename や大規模リファクタは禁止
- public interface は極力維持
- 一時しのぎより、保守しやすい修正を優先
- 変更は `apply_patch` を使って最小差分で行う

## 検証
- 可能なら関連テストやビルドを実行
- 実行できない場合は、できない理由と未確認点を明記
- UI 変更時は見た目だけでなく、アクセシビリティと状態更新の影響も確認

## 進め方
- 要約 → 方針 → 最小ステップ → コード
- 修正後に既存挙動への影響を確認
- 最後に必要な場合だけ、ユーザー側でのビルド確認を促す

## 返答
- 日本語で簡潔に
- 差分中心で説明
- 未確認事項は断定しない

